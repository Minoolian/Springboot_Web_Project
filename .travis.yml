language: java
jdk:
  - openjdk8

branches:
  only:
    - master

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

// 권한 추가를 위한 설정
before_install: 
  - chmod +x gradlew 

notifications:
  email:
    recipients:
      - mw.lee@kakao.com
